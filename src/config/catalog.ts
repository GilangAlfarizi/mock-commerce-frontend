/** Page size for public product listing UI (≤ API max 50). */
export const PUBLIC_PAGE_SIZE = 9;

/** Number of cards shown on the home highlight section. */
export const HIGHLIGHT_PRODUCT_COUNT = 3;
